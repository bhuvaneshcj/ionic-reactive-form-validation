import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ionicforms.in',
  appName: 'Reactiveforms',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
